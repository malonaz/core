{{ $requiresSessionManager := false }}
{{- range $server := .servers -}}
    {{- if eq $server.type "grpc" -}}
        {{- range $interceptor := $server.interceptors -}}
            {{- if eq $interceptor.type "permission_authentication" -}}
                {{ $requiresSessionManager = true }}
            {{ end }}
        {{ end }}
    {{ end }}
{{- end -}}
// Code generated by 'github.com/malonaz/core/tools/template'. DO NOT EDIT.
// This file is automatically generated from templates/main.go.tmpl
// To make changes, edit the template file and regenerate.
package main

{{ range $server := .servers -}}
    {{- if and (eq $server.type "grpc") $server.descriptorSet -}}
        {{- if doOnce "embed_import" -}}
            {{ $_ := goImportAlias "embed" "_" }}
        {{ end }}
        //go:embed {{ $server.descriptorSet }}
        var {{ $server.name | camelcase | untitle }}DescriptorSetBytes []byte
    {{ end }}
{{- end -}}

var opts struct {
    {{ template "mainOpts" . }}
}


func main() {
    ctx := {{ goImport "context" }}.Background()
    if err := run(ctx); err != nil {
        {{ goImport "log/slog" }}.ErrorContext(ctx, "running", "error", err)
        {{ goImport "os" }}.Exit(1)
    }
}

func run(ctx {{ goImport "context" }}.Context) error {
    {{ goImport "math/rand" }}.Seed({{ goImport "time"}}.Now().UnixNano())
    if err := {{ goImport "github.com/malonaz/core/go/flags" }}.Parse(&opts); err != nil {
        return err
    }
    if err := {{ goImport "github.com/malonaz/core/go/logging" }}.Init(opts.Logging); err != nil {
        return err
    }
    {{ if .setup }}
    if err := setup(ctx); err != nil {
        return {{ goImport "fmt" }}.Errorf("setup: %w", err)
    }
    {{ end }}

    prometheusServer := {{ goImport "github.com/malonaz/core/go/prometheus" }}.NewServer(opts.Prometheus)
    go prometheusServer.Start(ctx)
    defer prometheusServer.Stop(ctx)

    {{ if $requiresSessionManager }}
    sessionManager := {{ goImport "github.com/malonaz/core/go/authentication" }}.NewSessionManager(opts.SessionManager)
    {{ end }}

    errChan := make(chan error, 1)
    runServer := func(ctx {{ goImport "context" }}.Context, serverName string, serveFn func({{ goImport "context" }}.Context) error) {
        go func() {
            if err := serveFn(ctx); err != nil {
                errChan <- {{ goImport "fmt" }}.Errorf("serving %s: %w", serverName, err)
            }
        }()
    }

    {{- /* GATHER SERVICE MANIFESTS. serviceGraph resolves every service the servers host and
           everything those services depend on into levels: a service in one level depends only
           on services in earlier levels, so each level can be started once the previous one is
           up. $serviceManifests flattens the levels for the passes below that only care about a
           service's non-service dependencies. */ -}}
    {{- $serviceLevels := serviceGraph .servers -}}
    {{- $serviceManifests := list -}}
    {{- $hasConcurrentLevel := false -}}
    {{- range $level := $serviceLevels -}}
        {{- if gt (len $level) 1 -}}{{- $hasConcurrentLevel = true -}}{{- end -}}
        {{- range $serviceManifest := $level -}}
            {{- $serviceManifests = append $serviceManifests $serviceManifest -}}
        {{- end -}}
    {{- end -}}

    {{- /* Map each hosted service to the servers hosting it, then hand those servers down to
           the services it depends on, so that a dependency's health checks are reported by
           every server that ultimately relies on it. */ -}}
    {{- $rootNameToServerNames := dict -}}
    {{- range $server := .servers -}}
        {{- $rootTargets := list -}}
        {{- range $service := $server.services -}}
            {{- $rootTargets = append $rootTargets $service.manifest -}}
        {{- end -}}
        {{- if $server.manifest -}}
            {{- $rootTargets = append $rootTargets $server.manifest -}}
        {{- end -}}
        {{- range $rootTarget := $rootTargets -}}
            {{- $rootName := (parseYaml $rootTarget).name -}}
            {{- $existingServers := index $rootNameToServerNames $rootName | default (list) -}}
            {{- if not (has $server.name $existingServers) -}}
                {{- $rootNameToServerNames = set $rootNameToServerNames $rootName (append $existingServers $server.name) -}}
            {{- end -}}
        {{- end -}}
    {{- end -}}
    {{- $serviceNameToServerNames := dict -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- $serverNames := list -}}
        {{- range $rootName := $serviceManifest.roots -}}
            {{- range $serverName := index $rootNameToServerNames $rootName | default (list) -}}
                {{- if not (has $serverName $serverNames) -}}
                    {{- $serverNames = append $serverNames $serverName -}}
                {{- end -}}
            {{- end -}}
        {{- end -}}
        {{- $serviceNameToServerNames = set $serviceNameToServerNames $serviceManifest.name $serverNames -}}
    {{- end -}}

    {{- $serverNameToServiceNameToServedProtos := dict -}}
    {{- range $server := .servers -}}
        {{- if eq $server.type "grpc" -}}
            {{- range $service := $server.services -}}
                {{- $serviceManifest := parseYaml $service.manifest -}}
                {{- $existing := index $serverNameToServiceNameToServedProtos $server.name | default (dict) -}}
                {{- $protos := index $existing $serviceManifest.name | default (list) -}}
                {{- $protos = append $protos $service.proto -}}
                {{- $existing = set $existing $serviceManifest.name $protos -}}
                {{- $serverNameToServiceNameToServedProtos = set $serverNameToServiceNameToServedProtos $server.name $existing -}}
            {{- end -}}
        {{- end -}}
    {{- end -}}

    {{- $serverNameToServiceNameToHealthCheckFunctions := dict -}}

    {{- /* DEPENDENCIES GRPC CONNECTIONS */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $dep := $serviceManifest.dependencies -}}
            {{- if eq $dep.type "grpc_client" -}}
                {{- if doOnce "grpc_conns_instantiations" -}}
                    {{ "\n\n// Instantiate GRPC Connections (using one per identical endpoint)\n" -}}
                    endpointToGRPCConnection := map[string]*{{ goImport "github.com/malonaz/core/go/grpc" }}.Connection{}
                {{- end -}}

                {{- if doOnce (printf "grpc_conn_deps_%s_%s" $dep.proto $dep.service) -}}
                    {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
                    if _, ok := endpointToGRPCConnection[opts.{{ $grpc.OptsFieldName }}.Endpoint()]; !ok {
                        connection, err := {{ $grpc.NewConnection }}
                        if err != nil {
                            return {{ goImport "fmt" }}.Errorf("instantiating new connection [%s]: %v", "{{ $dep.proto }}", err)
                        }
                        if err := connection.Connect(ctx); err != nil {
                            return {{ goImport "fmt" }}.Errorf("connecting [%s]: %v", "{{ $dep.proto }}", err)
                        }
                        defer connection.Close()
                        endpointToGRPCConnection[opts.{{ $grpc.OptsFieldName }}.Endpoint()] = connection
                    }
                {{- end }}
            {{- end }}
        {{- end }}
    {{- end }}

    {{- /* DEPENDENCIES GRPC CLIENTS */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $dep := $serviceManifest.dependencies -}}
            {{- if eq $dep.type "grpc_client" -}}
                {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
                {{- if doOnce "grpc_client_instantiations" -}}{{ "\n\n// Instantiate GRPC Clients" -}}{{- end -}}
                {{- if doOnce (printf "grpc_deps_%s_%s" $dep.proto $dep.service) }}
                    {{ $grpc.Connection }}, ok := endpointToGRPCConnection[opts.{{ $grpc.OptsFieldName }}.Endpoint()]
                    if !ok {
                        return {{ goImport "fmt" }}.Errorf("could not find grpc connection on for {{ $grpc.HumanName }}")
                    }
                    {{ $grpc.Client }} := {{ $grpc.NewClient }}
                    {{ $grpc.HealthCheck }} := {{ $grpc.Connection }}.HealthCheckFn({{ $grpc.ServiceDescriptionName }})
                    // Only consumed by services that declare this one as a dependency, of which a
                    // server composing a subset of the services may have none.
                    _ = {{ $grpc.HealthCheck }}
                {{ end }}

                {{- /* Add health check function to all servers that use this service, unless self-referential */ -}}
                {{- $serverNames := index $serviceNameToServerNames $serviceManifest.name -}}
                {{- range $serverName := $serverNames -}}
                    {{- $serviceNameToServedProtos := index $serverNameToServiceNameToServedProtos $serverName | default (dict) -}}
                    {{- $servedProtos := index $serviceNameToServedProtos $serviceManifest.name | default (list) -}}
                    {{- if has $dep.proto $servedProtos -}}{{- continue -}}{{- end -}}
                    {{- $serverHealthChecks := index $serverNameToServiceNameToHealthCheckFunctions $serverName | default (dict) -}}
                    {{- $existingHealthChecks := index $serverHealthChecks $serviceManifest.name | default (list) -}}
                    {{- $updatedHealthChecks := append $existingHealthChecks $grpc.HealthCheck -}}
                    {{- $serverHealthChecks = set $serverHealthChecks $serviceManifest.name $updatedHealthChecks -}}
                    {{- $serverNameToServiceNameToHealthCheckFunctions = set $serverNameToServiceNameToHealthCheckFunctions $serverName $serverHealthChecks -}}
                {{- end -}}
            {{- end }}
        {{- end }}
    {{- end }}

    {{- /* DEPENDENCIES POSTGRES CONNECTIONS */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $dep := $serviceManifest.dependencies -}}
            {{- if or (eq $dep.type "postgres_db_client") (eq $dep.type "postgres_client") -}}
                {{ $database := default $dep.name $dep.database }}
                {{- if doOnce "postgres_db_client_instantiations" -}}
                    {{ "\n\n// Instantiate Postgres Connections (using one per identical endpoint)" }}
                    endpointToPostgresConnection := map[string]*{{ goImport "github.com/malonaz/core/go/postgres" }}.Client{}
                {{- end }}
                if _, ok := endpointToPostgresConnection[opts.{{ $database | camelcase }}Postgres.Endpoint()]; !ok {
                    connection := {{ goImport "github.com/malonaz/core/go/postgres" }}.NewClient(opts.{{ $database | camelcase }}Postgres)
                    if err := connection.Start(ctx); err != nil {
                        return {{ goImport "fmt" }}.Errorf("starting postgres connection ({{ $database }}): %w", err)
                    }
                    defer connection.Close()
                    endpointToPostgresConnection[opts.{{ $database | camelcase }}Postgres.Endpoint()] = connection
                }
            {{- end }}
        {{- end }}
    {{- end }}

    {{- /* DEPENDENCIES POSTGRES */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $dep := $serviceManifest.dependencies -}}
            {{- if or (eq $dep.type "postgres_db_client") (eq $dep.type "postgres_client") -}}
                {{ $database := default $dep.name $dep.database }}
                {{- $dbKey := printf "postgres_db_client_%s_%s" $database -}}
                {{- if doOnce $dbKey }}
                    {{- "\n" }}
                    // Instantiate {{ $database }} db client.
                    {{ $database | camelcase | untitle }}PsqlClient, ok := endpointToPostgresConnection[opts.{{ $database | camelcase }}Postgres.Endpoint()]
                    if !ok {
                        return {{ goImport "fmt" }}.Errorf("could not find postgres connection for {{ $database }}")
                    }
                {{- end }}

                {{- /* Add health check function to all servers that use this service */ -}}
                {{- $serverNames := index $serviceNameToServerNames $serviceManifest.name -}}
                {{- $healthCheckFn := printf "%sPsqlClient.Ping" ($database | camelcase | untitle) -}}
                {{- range $serverName := $serverNames -}}
                    {{- $healthCheckKey := printf "postgres_health_%s_%s_%s" $serverName $serviceManifest.name $database -}}
                    {{- if doOnce $healthCheckKey -}}
                        {{- $serverHealthChecks := index $serverNameToServiceNameToHealthCheckFunctions $serverName | default (dict) -}}
                        {{- $existingHealthChecks := index $serverHealthChecks $serviceManifest.name | default (list) -}}
                        {{- $updatedHealthChecks := append $existingHealthChecks $healthCheckFn -}}
                        {{- $serverHealthChecks = set $serverHealthChecks $serviceManifest.name $updatedHealthChecks -}}
                        {{- $serverNameToServiceNameToHealthCheckFunctions = set $serverNameToServiceNameToHealthCheckFunctions $serverName $serverHealthChecks -}}
                    {{- end -}}
                {{- end -}}

                {{- if eq $dep.type "postgres_db_client" -}}
                    {{- $dbImplKey := printf "postgres_impl_%s_%s" $database $dep.implementation -}}
                    {{- if doOnce $dbImplKey }}
                        {{ $database | camelcase | untitle }}_{{ $dep.implementation | replace "/" "_" | replace "." "_" | replace ":" "_" }}DBClient := {{ plzGoImport $dep.implementation }}.New({{ $database | camelcase | untitle }}PsqlClient)
                    {{ end }}
                {{- end }}
            {{- end }}
        {{- end }}
    {{- end }}

    {{- /* DEPENDENCIES NATS CLIENT */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $dep := $serviceManifest.dependencies -}}
            {{- if eq $dep.type "nats" -}}
                {{- if doOnce "nats_client_instantiation" }}

                    // Instantiate NATS client.
                    natsClient, err := {{ goImport "github.com/malonaz/core/go/nats" }}.NewClient(opts.Nats)
                    if err != nil {
                        return {{ goImport "fmt" }}.Errorf("instantiating nats client: %w", err)
                    }
                    if err := natsClient.Start(ctx); err != nil {
                        return {{ goImport "fmt" }}.Errorf("starting nats client: %w", err)
                    }
                    defer natsClient.Close()
                {{- end -}}
            {{- end -}}
        {{- end -}}
    {{- end }}

    {{- /* INSTANTIATE EVERY SERVICE. $serviceManifests is the dependency levels flattened, so a
           service is always declared after the services it depends on. */ -}}
    {{- range $serviceManifest := $serviceManifests }}
        {{- $serviceName := $serviceManifest.name | camelcase | untitle }}

        {{ if $requiresSessionManager -}}
        {{ $serviceName }}WithServiceAccount, err := opts.InternalServiceAuthentication.WithServiceAccount("{{ $serviceManifest.name }}")
        if err != nil {
            return {{ goImport "fmt" }}.Errorf("with service account: {{ $serviceManifest.name }}: %v", err)
        }
        {{- else -}}
        {{ $serviceName }}WithServiceAccount := func(ctx {{ goImport "context" }}.Context) {{ goImport "context" }}.Context { return ctx }
        {{- end }}

        // Instantiate {{ $serviceName }}.
        {{ $serviceName }}, err := {{ plzGoImport $serviceManifest.implementation }}.New(
            opts.{{ $serviceName | title }},
            {{- range $dep := $serviceManifest.dependencies }}
                {{- if eq $dep.type "grpc_client" }}
                    {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") -}}
                    {{ $grpc.Client }},
                {{- else if eq $dep.type "postgres_db_client" }}
                    {{ $database := default $dep.name $dep.database }}
                    {{ $database | camelcase | untitle }}_{{ $dep.implementation | replace "/" "_" | replace "." "_" | replace ":" "_" }}DBClient,
                {{- else if eq $dep.type "postgres_client" }}
                    {{ $database := default $dep.name $dep.database }}
                    {{ $database | camelcase | untitle }}PsqlClient,
                {{- else if eq $dep.type "service" }}
                    {{ $dep.name | camelcase | untitle }},
                {{- else if eq $dep.type "nats" }}
                    natsClient,
                {{- end }}
            {{- end }}
        )
        if err != nil {
            return {{ goImport "fmt" }}.Errorf("instantiating {{ $serviceName }}: %v", err)
        }
    {{- end }}

    {{- /* NATS STREAMS. A service creating a consumer needs the stream it consumes to exist, and
           stream ownership is not something the manifests declare, so no dependency level can
           order the two. Create every stream this binary's services own before any of them
           starts, which orders them all at once. */ -}}
    {{- range $serviceManifest := $serviceManifests -}}
        {{- range $codegen := $serviceManifest.codegens -}}
            {{- if and (eq $codegen.type "rpc") $codegen.nats -}}
                {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                {{ if doOnce "nats_streams_comment" }}
    // Create the NATS streams the services own, before any service starts a consumer on one.
                {{- end }}
    if err := {{ $serviceName }}.CreateStreams(ctx); err != nil {
        return {{ goImport "fmt" }}.Errorf("creating {{ $serviceManifest.name }} nats streams: %w", err)
    }
            {{- end -}}
        {{- end -}}
    {{- end }}

    {{- /* START THE SERVICES, ONE DEPENDENCY LEVEL AT A TIME. Every service is started before any
           server begins serving, and a service only starts once every service it depends on has
           finished starting. */ -}}
    {{ "\n" }}
    // Service cleanups run in reverse start order, so that a service is always torn down
    // before anything it depends on.
    {{- if $hasConcurrentLevel }}
    var serviceCleanupMutex {{ goImport "sync" }}.Mutex
    {{- end }}
    var serviceCleanupFns []func()
    addServiceCleanup := func(cleanup func()) {
        {{- if $hasConcurrentLevel }}
        serviceCleanupMutex.Lock()
        defer serviceCleanupMutex.Unlock()
        {{- end }}
        serviceCleanupFns = append(serviceCleanupFns, cleanup)
    }
    defer func() {
        for i := len(serviceCleanupFns) - 1; i >= 0; i-- {
            serviceCleanupFns[i]()
        }
    }()

    {{- range $levelIndex, $level := $serviceLevels }}
        {{- if gt (len $level) 1 }}
            // Start dependency level {{ $levelIndex }} in step: no service in it depends on
            // another, and every service they do depend on is already started.
            {
                levelErrs := make([]error, {{ len $level }})
                var levelWaitGroup {{ goImport "sync" }}.WaitGroup
                levelWaitGroup.Add({{ len $level }})
                {{- range $serviceIndex, $serviceManifest := $level }}
                    {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                    go func() {
                        defer levelWaitGroup.Done()
                        cleanup, err := {{ $serviceName }}.Start(ctx, {{ $serviceName }}WithServiceAccount)
                        if err != nil {
                            levelErrs[{{ $serviceIndex }}] = {{ goImport "fmt" }}.Errorf("starting {{ $serviceName }} service: %w", err)
                            return
                        }
                        addServiceCleanup(cleanup)
                    }()
                {{- end }}
                levelWaitGroup.Wait()
                if err := {{ goImport "errors" }}.Join(levelErrs...); err != nil {
                    return err
                }
            }
        {{- else }}
            {{- range $serviceManifest := $level }}
                {{- $serviceName := $serviceManifest.name | camelcase | untitle }}

                // Start {{ $serviceName }} service.
                {{ $serviceName }}Cleanup, err := {{ $serviceName }}.Start(ctx, {{ $serviceName }}WithServiceAccount)
                if err != nil {
                    return {{ goImport "fmt" }}.Errorf("starting {{ $serviceName }} service: %v", err)
                }
                addServiceCleanup({{ $serviceName }}Cleanup)
            {{- end }}
        {{- end }}
    {{- end }}

    healthServer := {{ goImport "github.com/malonaz/core/go/health" }}.NewServer(opts.Health, "{{ .name }}")
    var gracefulStopFns []func() error
    var stopFns []func() error

    {{ range $server := .servers }}
        // Start {{ $server.name | camelcase }} Server
        {
        {{- if eq $server.type "processor" }}
            {{- $serviceManifest := parseYaml $server.manifest -}}
            {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
            healthServer.RegisterService("{{ $server.name }}", {{ $serviceName }}.HealthCheck)
        {{- end }}

        {{- if eq $server.type "grpc" }}
            register := func(server *{{ goImport "github.com/malonaz/core/go/grpc" }}.Server) {
                {{- range $service := $server.services }}
                    {{- $serviceManifest := parseYaml $service.manifest -}}
                    {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                    {{ $grpc := parseGRPC $service.proto $service.service -}}
                    {{ $grpc.Register $serviceName }}
                {{- end }}
            }

            {{- range $interceptor := $server.interceptors -}}
                {{ if eq $interceptor.type "external_api_key_authentication" }}
                    {{ $interceptor.type | camelcase | untitle }}Interceptor, err := {{ goImport "github.com/malonaz/core/go/authentication" }}.New{{ $interceptor.type | camelcase }}Interceptor(opts.{{ $interceptor.type | camelcase }}, sessionManager)
                    if err != nil {
                        return {{ goImport "fmt" }}.Errorf("instantiating {{ $interceptor.type | replace "_" " " }} interceptor: %v", err)
                    }
                {{ else if eq $interceptor.type "internal_service_authentication" }}
                    {{ $interceptor.type | camelcase | untitle }}Interceptor, err := {{ goImport "github.com/malonaz/core/go/authentication" }}.New{{ $interceptor.type | camelcase }}Interceptor(opts.{{ $interceptor.type | camelcase }}, sessionManager)
                    if err != nil {
                        return {{ goImport "fmt" }}.Errorf("instantiating {{ $interceptor.type | replace "_" " " }} interceptor: %v", err)
                    }
                {{ else if eq $interceptor.type "jwt_authentication" }}
                    {{ $interceptor.type | camelcase | untitle }}Interceptor, err := {{ goImport "github.com/malonaz/core/go/authentication" }}.New{{ $interceptor.type | camelcase }}Interceptor(ctx, opts.{{ $interceptor.type | camelcase }}, sessionManager)
                    if err != nil {
                        return {{ goImport "fmt" }}.Errorf("instantiating {{ $interceptor.type | replace "_" " " }} interceptor: %v", err)
                    }
                {{ else if eq $interceptor.type "permission_authentication" }}
                    {{ $interceptor.type | camelcase | untitle }}Interceptor, err := {{ goImport "github.com/malonaz/core/go/authentication" }}.New{{ $interceptor.type | camelcase }}Interceptor(opts.{{ $interceptor.type | camelcase }}, sessionManager)
                    if err != nil {
                        return {{ goImport "fmt" }}.Errorf("instantiating {{ $interceptor.type | replace "_" " " }} interceptor: %v", err)
                    }
                {{ else if eq $interceptor.type "session_local_context_injector" -}}
                {{ else if eq $interceptor.type "session_log_injector" -}}
                {{ else if eq $interceptor.type "session_outgoing_context_injector" -}}
                {{ else }}
                    {{ fail (printf "Unknown interceptor type: %s" $interceptor.type) }}
                {{ end }}
            {{- end }}

            grpcServer := {{ goImport "github.com/malonaz/core/go/grpc" }}.NewServer(opts.{{ $server.name | camelcase }}GRPC, opts.Certs, opts.Prometheus, "{{ $server.name }}", register)
                {{- if $server.descriptorSet -}}
                    .WithFileDescriptorSet({{ $server.name | camelcase | untitle }}DescriptorSetBytes)
                {{- end }}

            grpcServer.WithUnaryInterceptors(
                {{- range $interceptor := $server.interceptors -}}
                    {{- if eq $interceptor.type "session_local_context_injector" }}
                        sessionManager.UnaryServerLocalContextInjectorInterceptor(),
                    {{- else if eq $interceptor.type "external_api_key_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Unary(),
                    {{- else if eq $interceptor.type "internal_service_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Unary(),
                    {{- else if eq $interceptor.type "jwt_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Unary(),
                    {{- else if eq $interceptor.type "permission_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Unary(),
                    {{- else if eq $interceptor.type "session_log_injector" }}
                        sessionManager.UnaryServerLogInjectorInterceptor(),
                    {{- else if eq $interceptor.type "session_outgoing_context_injector" }}
                        sessionManager.UnaryServerOutgoingContextInjectorInterceptor(),
                    {{ else }}
                        {{ fail (printf "Unknown interceptor type: %s" $interceptor.type) }}
                    {{- end -}}
                {{- end -}}
            ).
            WithStreamInterceptors(
                {{- range $interceptor := $server.interceptors -}}
                    {{- if eq $interceptor.type "session_local_context_injector" }}
                        sessionManager.StreamServerLocalContextInjectorInterceptor(),
                    {{- else if eq $interceptor.type "external_api_key_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Stream(),
                    {{- else if eq $interceptor.type "internal_service_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Stream(),
                    {{- else if eq $interceptor.type "jwt_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Stream(),
                    {{- else if eq $interceptor.type "permission_authentication" }}
                        {{ $interceptor.type | camelcase | untitle }}Interceptor.Stream(),
                    {{- else if eq $interceptor.type "session_log_injector" }}
                        sessionManager.StreamServerLogInjectorInterceptor(),
                    {{- else if eq $interceptor.type "session_outgoing_context_injector" }}
                        sessionManager.StreamServerOutgoingContextInjectorInterceptor(),
                    {{ else }}
                        {{ fail (printf "Unknown interceptor type: %s" $interceptor.type) }}
                    {{- end -}}
                {{- end -}}
            )

            gracefulStopFns = append(gracefulStopFns, grpcServer.GracefulStop)
            stopFns = append(stopFns, grpcServer.Stop)

            {{- range $service := $server.services }}
                {{- $serviceManifest := parseYaml $service.manifest -}}
                {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                {{ $grpc := parseGRPC $service.proto $service.service -}}
                {{- $serverHealthChecks := index $serverNameToServiceNameToHealthCheckFunctions $server.name | default (dict) -}}
                {{- $serviceHealthChecks := index $serverHealthChecks $serviceManifest.name | default (list) -}}
                grpcServer.GetHealthServer().RegisterService(
                    {{- $grpc.ServiceDescriptionName -}},
                    {{- range $healthCheckFn := $serviceHealthChecks -}}
                        {{ $healthCheckFn }},
                    {{- end -}}
                )
            {{- end }}

            healthServer.RegisterService("{{ $server.name }}", grpcServer.GetHealthServer().CheckFn())
            runServer(ctx, "{{ $server.name }}", grpcServer.Serve)

            {{- $hasGateway := false -}}
            {{- range $service := $server.services -}}
                {{- if $service.gateway -}}
                    {{- $grpc := parseGRPC $service.proto $service.service -}}
                    {{- if doOnce $server.name }}
                        {{ $hasGateway = true }}
                        // GRPC gateway handlers.
                        var registerHandlers = []{{ goImport "github.com/malonaz/core/go/grpc" }}.RegisterHandler{
                    {{- end }}
                    {{ $grpc.RegisterHandlerFromEndpoint }},
                {{ end }}
            {{ end }}
            {{- if $hasGateway -}}
                }
                grpcGatewayServer := grpc.NewGateway(opts.{{ $server.name | camelcase }}GRPCGateway, opts.{{ $server.name | camelcase }}GRPC, opts.Certs, opts.Prometheus, registerHandlers)
                runServer(ctx, "{{ $server.name }}", grpcGatewayServer.Serve)
            {{- end }}
        {{- end }}

        {{- if eq $server.type "http" }}
            register := func(server *{{ goImport "github.com/malonaz/core/go/http" }}.Server) {
                {{- range $service := $server.services }}
                    {{- $serviceManifest := parseYaml $service.manifest -}}
                    {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                    {{ $serviceName }}.RegisterRoutes(server)
                {{- end }}
            }

            httpServer := {{ goImport "github.com/malonaz/core/go/http" }}.NewServer(opts.{{ $server.name | camelcase }}HTTP, "{{ $server.name }}", register)
            gracefulStopFns = append(gracefulStopFns, httpServer.GracefulStop)
            stopFns = append(stopFns, httpServer.Stop)

            {{- range $service := $server.services }}
                {{- $serviceManifest := parseYaml $service.manifest -}}
                {{- $serviceName := $serviceManifest.name | camelcase | untitle }}
                {{- $serverHealthChecks := index $serverNameToServiceNameToHealthCheckFunctions $server.name | default (dict) -}}
                {{- $serviceHealthChecks := index $serverHealthChecks $serviceManifest.name | default (list) }}
                httpServer.GetHealthServer().RegisterService(
                    "{{- $serviceName | kebabcase }}",
                    {{- range $healthCheckFn := $serviceHealthChecks -}}
                        {{ $healthCheckFn }},
                    {{- end -}}
                )
            {{- end }}

            healthServer.RegisterService("{{ $server.name }}", httpServer.GetHealthServer().CheckFn())
            runServer(ctx, "{{ $server.name }}", httpServer.Serve)
        {{- end }}

        {{- if eq $server.type "grpcwebproxy" }}
            grpcWebProxyServer := {{ goImport "github.com/malonaz/core/go/grpc/grpcwebproxy" }}.NewServer(opts.{{ $server.name | camelcase }}GRPCWebProxy, opts.Certs, opts.Prometheus)
            gracefulStopFns = append(gracefulStopFns, grpcWebProxyServer.GracefulStop)
            stopFns = append(stopFns, grpcWebProxyServer.Stop)
            healthServer.RegisterService("{{ $server.name }}", grpcWebProxyServer.HealthCheckFn())
            runServer(ctx, "{{ $server.name }}", grpcWebProxyServer.Serve)
        {{- end }}
        }
    {{- end }}

    // Setup health check.
    go healthServer.Serve(ctx)

    gracefulStopFns = append(gracefulStopFns, func() error {
        healthServer.Shutdown()
        return nil
    })
    healthServer.MarkReady()
    handleSignals(ctx, errChan, gracefulStopFns, stopFns)
    return nil
}

{{ template "shutdownOpts" . }}
