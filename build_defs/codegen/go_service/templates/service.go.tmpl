// Code generated by 'github.com/malonaz/core/tools/template'. DO NOT EDIT.
// This file is automatically generated from templates/service.go.tmpl
// To make changes, edit the template file and regenerate.
package {{ .implementation | base }}
{{- $exportFields := not (empty .exportServiceFields) }}
{{- /* Long-running codegens need the scheduler client the manifest must declare as a grpc_client dependency. */ -}}
{{- $longrunning := false }}
{{- range $codegen := .codegens }}{{ if $codegen.longrunning }}{{ $longrunning = true }}{{ end }}{{ end }}
{{- $schedulerClient := "" }}
{{- range $dep := .dependencies }}
  {{- if and (eq $dep.type "grpc_client") (eq $dep.service "scheduler-service") }}
    {{- $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
    {{- $schedulerClient = $grpc.Client }}
  {{- end }}
{{- end }}
{{- if and $longrunning (empty $schedulerClient) }}
  {{ fail "a longrunning codegen requires a grpc_client dependency on scheduler-service" }}
{{- end }}

{{ if .includeRuntime }}
type Opts struct{}

type runtime struct{}

func newRuntime(opts *Opts) (*runtime, error) {
    return &runtime{}, nil
}

func (s *Service) start(ctx {{ goImport "context" }}.Context) (func(), error) { return func() {}, nil }
{{ end }}

type Service struct {
  *runtime
  {{- range $codegen := .codegens }}
      {{- if eq $codegen.type "rpc" }}
        *{{ plzGoImport $codegen.implementation }}.{{ $codegen.name | camelcase }}Server
    {{- else }}
      {{ fail (printf "%s unknown type" $codegen.type) }}
    {{- end }}
  {{- end }}
  {{- if $longrunning }}
  // Serves google.longrunning.Operations for the long-running methods above.
  *{{ goImport "github.com/malonaz/core/go/scheduler/longrunning" }}.Server
  {{- end }}
  log *{{ goImport "log/slog" }}.Logger
  {{ ternary "Opts" "opts" $exportFields }} *Opts
  {{ ternary "With" "with" $exportFields }}ServiceAccount func({{ goImport "context" }}.Context) {{ goImport "context" }}.Context
  {{- range $dep := .dependencies }}
    {{- if eq $dep.type "grpc_client" }}
        {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
        {{ ternary ($grpc.Client | title) $grpc.Client $exportFields }} {{ $grpc.ClientInterface }}
    {{- else if eq $dep.type "postgres_db_client" }}
        {{ ternary ($dep.name | title) $dep.name $exportFields }}PostgresStore *{{ plzGoImport $dep.implementation }}.Store
    {{- else if eq $dep.type "postgres_client" }}
        {{ ternary ($dep.name | title) $dep.name $exportFields }}PostgresClient *{{ goImport "github.com/malonaz/core/go/postgres" }}.Client
    {{- else if eq $dep.type "service" }}
        {{ ternary ($dep.name | title) $dep.name $exportFields }}Service *{{ plzGoImport $dep.implementation }}.Service
    {{- else if eq $dep.type "nats" }}
        {{ ternary "NatsClient" "natsClient" $exportFields }} *{{ goImport "github.com/malonaz/core/go/nats" }}.Client
    {{- end }}
  {{- end }}
}

func (s *Service) WithLogger(logger *{{ goImport "log/slog" }}.Logger) *Service {
  s.log = logger
  return s
}

func New(
  opts *Opts,
      {{- range $dep := .dependencies }}
            {{- if eq $dep.type "grpc_client" }}
                {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
                {{ $grpc.Client }} {{ $grpc.ClientInterface }},
            {{- else if eq $dep.type "postgres_db_client" }}
                {{ $dep.name }}PostgresStore *{{ plzGoImport $dep.implementation }}.Store,
            {{- else if eq $dep.type "postgres_client" }}
                {{ $dep.name }}PostgresClient *{{ goImport "github.com/malonaz/core/go/postgres" }}.Client,
           {{- else if eq $dep.type "service" }}
                {{ $dep.name }}Service *{{ plzGoImport $dep.implementation }}.Service,
           {{- else if eq $dep.type "nats" }}
                natsClient *{{ goImport "github.com/malonaz/core/go/nats" }}.Client,
            {{- end }}
        {{- end }}

) (*Service, error) {
  runtime, err := newRuntime(opts)
  if err != nil {
    return nil, {{ goImport "fmt" }}.Errorf("instantiating runtime: %w", err)
  }
  service := &Service{
    runtime: runtime,
     log: {{ goImport "log/slog" }}.Default(),
    {{ ternary "Opts" "opts" $exportFields }}: opts,
      {{- range $dep := .dependencies }}
            {{- if eq $dep.type "grpc_client" }}
                {{ $grpc := parseGRPC $dep.proto $dep.service ($dep.name | default "") }}
                {{ ternary ($grpc.Client | title) $grpc.Client $exportFields }}: {{ $grpc.Client }},
            {{- else if eq $dep.type "postgres_db_client" }}
                {{ ternary ($dep.name | title) $dep.name $exportFields }}PostgresStore: {{ $dep.name }}PostgresStore,
            {{- else if eq $dep.type "postgres_client" }}
                {{ ternary ($dep.name | title) $dep.name $exportFields }}PostgresClient: {{ $dep.name }}PostgresClient,
            {{- else if eq $dep.type "service" }}
                {{ ternary ($dep.name | title) $dep.name $exportFields }}Service: {{ $dep.name }}Service,
            {{- else if eq $dep.type "nats" }}
                {{ ternary "NatsClient" "natsClient" $exportFields }}: natsClient,
            {{- end }}
        {{- end }}
  }
  {{- range $codegen := .codegens }}
      {{- if eq $codegen.type "rpc" }}
  {{- /* The service itself is the runner: it implements Run{Method} for each long-running method. */}}
  service.{{ $codegen.name | camelcase }}Server = {{ plzGoImport $codegen.implementation }}.New{{ $codegen.name | camelcase }}Server({{ $codegen.store | camelcase | untitle }}PostgresStore{{ if $codegen.nats }}, natsClient{{ end }}{{ if $codegen.longrunning }}, {{ $schedulerClient }}, service{{ end }})
      {{- end }}
  {{- end }}
  {{- if $longrunning }}
  service.Server = {{ goImport "github.com/malonaz/core/go/scheduler/longrunning" }}.NewServer({{ $schedulerClient }}, {{ goImport "slices" }}.Concat(
    {{- range $codegen := .codegens }}{{ if $codegen.longrunning }}
    {{ plzGoImport $codegen.implementation }}.{{ $codegen.name | camelcase }}LongrunningMethods,
    {{- end }}{{ end }}
  ))
  {{- end }}
  return service, nil
}

func (s *Service) Start(ctx {{ goImport "context" }}.Context, withServiceAccount func({{ goImport "context" }}.Context) {{ goImport "context" }}.Context) (func(), error) {
  if withServiceAccount != nil {
    s.{{ ternary "With" "with" $exportFields }}ServiceAccount = withServiceAccount
    ctx = withServiceAccount(ctx)
  }
  {{- range $codegen := .codegens }}
    {{- if eq $codegen.type "rpc" }}
  if err := s.{{ $codegen.name | camelcase }}Server.Start(ctx); err != nil {
    return nil, {{ goImport "fmt" }}.Errorf("starting {{ $codegen.name }} server: %w", err)
  }
    {{- end }}
  {{- end }}
  return s.start(ctx)
}
