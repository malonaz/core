{{- /* ################### TOPLINE VARIABLES ################### */ -}}
{{ $file := .File }}
{{ $generatedFile := .GeneratedFile -}}
{{- $modelGpi := .AdditionalGoImportPaths.model -}}

// Code generated by protoc-templates. DO NOT EDIT.
// source: {{ $file.Desc.Path }}
package {{ .PackageName }}

{{- $serviceInterfaces := dict }}
{{- $serviceToNatsStreamNames := dict }}
{{- $resourceTypeToRequiresNatsClient := dict }}
{{- range $service := $file.Services }}
    {{ $natsStreamOpts := getExt $service.Desc "malonaz.codegen.nats.v1.stream" }}
    {{ if $natsStreamOpts }}
        {{ range $opts := $natsStreamOpts }}
            {{ $natsStreamName := $opts.GetName | trimPrefix (toString $file.Desc.Package) | trimPrefix "." | replace "." "-" | camelcase | printf "Stream%s" }}
            {{ $serviceToNatsStreamNames = merge $serviceToNatsStreamNames (dict $service.GoName (append (default list (get $serviceToNatsStreamNames $service.GoName)) $natsStreamName)) }}
        {{ end }}
    {{ end }}
    {{- $resourceList := list }}
    {{- range $method := $service.Methods }}
        {{- $rpc := parseRPC $method }}
        {{- if $rpc }}
            {{- $pr := $rpc.ParsedResource }}
            {{ $natsEventOpts := getExt $rpc.Message.Desc "malonaz.codegen.nats.v1.event" }}
            {{ if $natsEventOpts }}
                {{ if not $natsStreamOpts }}{{ fail (printf "Resource %s has nats event annotation but service %s defines no such stream" $pr.Desc.Type $service.GoName) }}{{ end }}
                {{- $resourceTypeToRequiresNatsClient := set $resourceTypeToRequiresNatsClient $pr.Desc.Type true }}
            {{ end }}
            {{- $found := false }}
            {{- range $existingResource := $resourceList }}
                {{- if eq $existingResource.Desc.Singular $pr.Desc.Singular }}
                    {{- $found = true }}
                {{- end }}
            {{- end }}
            {{- if not $found }}
                {{- $resourceList = append $resourceList $pr }}
            {{- end }}
        {{- end }}
    {{- end }}
    {{- if gt (len $resourceList) 0 }}
        {{- $serviceName := $service.GoName }}
        {{- $serviceInterfaces = set $serviceInterfaces $serviceName $resourceList }}
    {{- end }}
{{- end }}

{{- range $serviceName, $resources := $serviceInterfaces }}
    type {{ $serviceName | untitle }}Store interface {
    {{- range $pr := $resources }}
        {{ $serviceName | untitle }}_{{ $pr.SingularGoName }}Store
    {{- end }}
    }

    {{ $natsStreamNames := get $serviceToNatsStreamNames $serviceName }}
    type {{ $serviceName }}Server struct {
    {{ if $natsStreamNames }}
        natsClient *{{ fqnCore "go/nats" "Client"}}
    {{ end }}
    {{- range $pr := $resources }}
        *{{ $serviceName | untitle }}_{{ $pr.SingularGoName }}Server
    {{- end }}
    }



    {{ $natsStreamNames := get $serviceToNatsStreamNames $serviceName }}
    func New{{ $serviceName }}Server(store {{ $serviceName | untitle }}Store{{ if $natsStreamNames }}, natsClient *{{ fqnCore "go/nats" "Client" }}{{ end }}) *{{ $serviceName }}Server {
    return &{{ $serviceName }}Server{
    {{ if $natsStreamNames }}
        natsClient: natsClient,
    {{ end }}
    {{- range $pr := $resources }}
        {{ $serviceName | untitle }}_{{ $pr.SingularGoName }}Server: new{{ $serviceName }}_{{ $pr.SingularGoName }}Server(store{{ if get $resourceTypeToRequiresNatsClient $pr.Desc.Type }}, natsClient{{ end }}),
    {{- end }}
    }
    }

    func (s *{{ $serviceName }}Server) Start(ctx {{ fqn "context" "Context"}}) error {
    {{ if $natsStreamNames }}
        {{ range $natsStreamName := $natsStreamNames }}
            if _, err := s.natsClient.CreateOrUpdateStream(ctx, {{ qgi ($file.GoImportPath.Ident (printf "Get%s%s" $serviceName $natsStreamName)) }}()); err != nil {
			  return {{ fqn "fmt"  "Errorf" }}("creating stream: %w", err)
		    }
    {{ end }}
    {{ end }}
      return nil
    }

{{- end }}


{{- range $service := $file.Services }}
    {{- range $method := $service.Methods }}
        {{ $rpc := parseRPC $method }}
        {{ if not $rpc }}{{ continue }}{{ end }}
        {{ $pr := $rpc.ParsedResource }}
        {{ $natsEventOpts := getExt $rpc.Message.Desc "malonaz.codegen.nats.v1.event" }}
        {{ $natsStreamName := $natsEventOpts.GetStream | trimPrefix (toString $file.Desc.Package) | trimPrefix "." | replace "." "-" | camelcase | printf "Stream%s" }}
        {{ $storeInterface := printf "%s_%sStore" ($service.Desc.Name | toString | camelcase | untitle) $pr.SingularGoName }}
        {{- $resourceGoName := $pr.SingularGoName }}
        {{- $modelGoName := printf "%sModel" $resourceGoName }}
        {{ $goTypeQgi := $rpc.Message.GoIdent.GoName | $modelGpi.Ident | qgi }}
        {{ $parseFromPb := (printf "%sFromPb" $rpc.Message.GoIdent.GoName) | $modelGpi.Ident | qgi }}
        {{ $parseName := (printf "Parse%sName" $rpc.Message.GoIdent.GoName) | $modelGpi.Ident | qgi }}
        {{ $goType := $rpc.Message.GoIdent.GoName }}
        {{ $errNotExistFqi := (printf "Err%sNotExist" $goType) | $modelGpi.Ident | qgi }}
        {{ $errEtagChangedFqi := (printf "Err%sETagChanged" $goType) | $modelGpi.Ident | qgi }}
        {{ $errAlreadyExistsFqi := (printf "Err%sAlreadyExists" $goType) | $modelGpi.Ident | qgi }}
        {{ $errAlreadyDeletedFqi := (printf "Err%sAlreadyDeleted" $goType) | $modelGpi.Ident | qgi }}
        {{- $singletonChildren := list }}
        {{- range $childPr := $pr.Children }}
            {{- if $childPr.Singleton }}
	              {{- $childMessage := getMessageUsingResourceType $childPr.Desc.Type }}
	              {{- $modelOpts := getModelOpts $childMessage }}
                {{- if $modelOpts }}
                    {{- $singletonChildren = append $singletonChildren $childPr }}
                {{ end }}
            {{- end }}
        {{ end }}
        {{- $softDeletable := $rpc.Message.Desc.Fields.ByName "delete_time" }}
        {{- $etag := $rpc.Message.Desc.Fields.ByName "etag" }}
        {{- $modelOpts := getModelOpts $rpc.Message -}}
        {{ $hasRequestID := false }}
        {{- range $method := $service.Methods -}}
            {{ $rpc := parseRPC $method }}
            {{ if not $rpc }}{{ continue }}{{ end }}
            {{ if $rpc.Create }}
                {{ $hasRequestID = not (empty ($method.Input.Desc.Fields.ByName "request_id")) }}
                {{ break }}
            {{ end }}
        {{- end -}}



        {{/* SERVER STRUCT */}}
        {{- $serverGoName := printf "%s_%sServer" ($service.GoName | untitle) $resourceGoName }}
        {{ if doOnce (printf "%s_resource_interface_def" $pr.Desc.Singular) }}
            type {{ $storeInterface }} interface {
            Insert{{ $resourceGoName }}{{ if $hasRequestID }}Idempotently{{ end }}(ctx {{ fqn "context" "Context" }}, {{ if $hasRequestID }}requestID string,{{ end }} {{ $resourceGoName | untitle }} *{{ $goTypeQgi }}{{ range $childPr := $singletonChildren }}, {{ $childPr.SingularGoName | untitle }} *{{ $childPr.SingularGoName | $modelGpi.Ident | qgi }}{{ end }}) (*{{ $goTypeQgi }}, error)
            Update{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, {{ $resourceGoName | untitle }} *{{ $goTypeQgi }}, updateClause string, columns []string{{ if $etag }}, etag string{{ end}}) (*{{ $goTypeQgi }}, error)
            {{ if not $pr.Singleton }}
                {{- if $softDeletable }}
                    SoftDelete{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, {{ $pr.PatternVariableIDs true }} string{{ if $etag }}, etag, newEtag string{{ end }}, deleteTime {{ fqn "time" "Time" }}) (*{{ $goTypeQgi }}, error)
                {{ else }}
                    Delete{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, {{ $pr.PatternVariableIDs true }} string{{ if $etag }}, etag string{{ end }}) (*{{ $goTypeQgi }}, error)
                {{- end }}
            {{ end }}
            Get{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, {{ $pr.PatternVariableIDs true }} string) (*{{ $goTypeQgi }}, error)
            List{{ $pr.PluralGoName }}(ctx {{ fqn "context" "Context" }}, {{ if $pr.Parent }}{{ $pr.Parent.PatternVariableIDs true }} string,{{ end }} {{ if $softDeletable }}showDeleted bool, {{ end }}whereClause, orderByClause, paginationClause string, dbColumns []string, whereParams ...any) ([]*{{ $goTypeQgi }}, error)
            }

            // Server implements the RPC interface.
            type {{ $serverGoName }} struct {
            store {{ $storeInterface }}
            {{ if $natsEventOpts }}
            natsClient *{{ fqnCore "go/nats" "Client"}}
            {{ end }}
            }

            // Instantiate a new server.
            func new{{ $serverGoName | title }}(store {{ $storeInterface }}{{ if $natsEventOpts }},natsClient *{{ fqnCore "go/nats" "Client"}}{{ end }}) *{{ $serverGoName }} {
            return &{{ $serverGoName }}{
            store: store,
            {{ if $natsEventOpts }}natsClient: natsClient,{{ end }}
            }
            }
        {{ end }}


        {{- if $rpc.Create }}
            // {{ printf "Create%s creates a %s" $resourceGoName $resourceGoName }}
            func (s *{{ $serverGoName }}) {{ $method.GoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
            // STEP 1: Set identifiers.
            {{ if $hasRequestID }}
                if request.RequestId == "" { // We always set a request id.
                request.RequestId = {{ fqnCore "go/uuid" "MustNewV7" }}().String()
                }
            {{ end }}
            {{ $pr.PatternVariableID true }} := request.{{ $resourceGoName }}Id
            if {{ $pr.PatternVariableID true }} == "" {
            {{ $pr.PatternVariableID true }} = {{ fqnCore "go/aip" "NewSystemGeneratedBase32ResourceID" }}()
            }

            {{ if $pr.Parent }}
                var {{ $pr.Parent.PatternVariableIDs true }} string
                if err := {{ fqn "go.einride.tech/aip/resourcename" "Sscan" }}(request.Parent, "{{ $pr.Parent.Pattern }}", {{ $pr.Parent.PatternVariableIDPtrs }}); err != nil {
		            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "invalid parent name: %v", err).Err()
                }
            {{ end }}
            request.{{ $resourceGoName }}.Name = {{ fqn "go.einride.tech/aip/resourcename" "Sprint" }}("{{ $pr.Pattern }}", {{ $pr.PatternVariableIDs true }})

            // STEP 2: Instantiate timestamps.
            {{ if $rpc.Message.Desc.Fields.ByName "create_time" -}}
                // Check for x-migration-request header
                if values := {{ fqn "google.golang.org/grpc/metadata" "ValueFromIncomingContext" }}(ctx, "x-migration-request"); len(values) > 0 {
                if request.{{ $resourceGoName }}.CreateTime == nil {
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "x-migration-request used without setting a create_time").Err()
                }
                } else {
                request.{{ $resourceGoName }}.CreateTime = {{ fqn "google.golang.org/protobuf/types/known/timestamppb" "Now" }}()
                }
            {{ end -}}
            {{ if $rpc.Message.Desc.Fields.ByName "update_time" -}}
                request.{{ $resourceGoName }}.UpdateTime = request.{{ $resourceGoName }}.CreateTime
            {{ end -}}

            {{- if $etag }}
                { // Capture the etag.
                var err error
                request.{{ $resourceGoName }}.Etag, err = {{ fqnCore "go/aip" "ComputeETag" }}(request.{{ $resourceGoName }})
                if err != nil {
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "computing etag: %v", err).Err()
                }
                }
            {{- end }}

            {{ "\n" }}
            // STEP 3: Convert the resource to the database representation.
            {{ $modelGoName | untitle }}, err := {{ $parseFromPb }}(request.{{ $resourceGoName }})
	          if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from pb to model: %v", err).Err()
	          }

            {{- range $childPr := $singletonChildren }}
                // Instantiate child singleton resource: {{ $childPr.Type }}
                {{- $childMessage := getMessageUsingResourceType $childPr.Desc.Type }}
                {{- $childResourceGoName := $childPr.SingularGoName -}}
                {{- if not $childMessage }}
                    {{- fail (printf "could not find message for child resource type %s" $childPr.Desc.Type) }}
                {{- end }}
                {{ $childResourceGoName | untitle }} := &{{ qgi $childMessage.GoIdent }}{
                Name: {{ fqn "go.einride.tech/aip/resourcename" "Sprint" }}("{{ $childPr.Pattern }}", {{ $childPr.PatternVariableIDs true }}),
                CreateTime: request.{{ $resourceGoName }}.CreateTime,
                UpdateTime: request.{{ $resourceGoName }}.UpdateTime,
                }

                {{- if $childMessage.Desc.Fields.ByName "etag" }}
                    { // Capture the etag.
                    var err error
                    {{ $childResourceGoName | untitle }}.Etag, err = {{ fqnCore "go/aip" "ComputeETag" }}({{ $childResourceGoName | untitle }})
                    if err != nil {
                    return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "computing %s etag: %v", {{ $childPr.Desc.Singular }}, err).Err()
                    }
                    }
                {{- end }}

                {{ $parseFromPb := (printf "%sFromPb" $childMessage.GoIdent.GoName) | $modelGpi.Ident | qgi }}
                {{ $childResourceGoName | untitle }}Model, err := {{ $parseFromPb }}({{ $childResourceGoName | untitle }})
                if err != nil {
		            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $childPr.Desc.Singular }} from pb to model: %v", err).Err()
	              }
            {{ end }}

            {{- if $method.Input.Desc.Fields.ByName "validate_only" }}
                if request.ValidateOnly {
                return  request.{{ $resourceGoName }}, nil
                }
            {{- end }}

            // STEP 4: Insert the resource idempotently.
            db{{ $modelGoName }}, err := s.store.Insert{{ $resourceGoName }}{{ if $hasRequestID }}Idempotently{{ end }}(ctx, {{ if $hasRequestID }}request.RequestId,{{ end }} {{ $modelGoName | untitle }}{{ range $childPr := $singletonChildren }}, {{ $childPr.SingularGoName | untitle }}Model{{ end }})
            if err != nil {
            if {{ fqn "errors" "Is" }}(err, {{ $errAlreadyExistsFqi }}) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "AlreadyExists" }}, "{{ $pr.Desc.Singular }} already exists").Err()
            }
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "inserting {{ $resourceGoName | snakecase }}: %v", err).Err()
	          }

            {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
		        if err != nil {
			      return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
		        }

            {{ if $natsEventOpts.GetCreated }}
              // STEP 5: Publish event.
                resourceCreatedEvent, err := {{ fqnCore "go/aip" "NewResourceCreatedEvent" }}({{ $resourceGoName | untitle }})
                if err != nil {
                  return nil, status.Errorf(codes.Internal, "constructing resource created event: %v", err).Err()
                }
                subject := {{ qgi ($file.GoImportPath.Ident (printf "Get%s%s" $service.GoName $natsStreamName)) }}().Subject("created")
               if err := s.natsClient.Publish(ctx, subject, resourceCreatedEvent); err != nil {
	                	return nil, status.Errorf(codes.Internal, "publishing resource created event: %v", err).Err()
	             }
            {{ end }}
            return {{ $resourceGoName | untitle }}, nil
            }

        {{- else if $rpc.Update }}
            var update{{ $resourceGoName }}RequestParser = {{ fqnCore "go/aip" "MustNewUpdateRequestParser" }}[*{{ qgi $method.Input.GoIdent }}, *{{ qgi $rpc.Message.GoIdent }}]()
            func (s *{{ $serverGoName }}) {{ $method.GoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
            if len(request.GetUpdateMask().GetPaths()) == 0 {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "missing update_mask.paths").Err()
            }
            if {{ fqn "go.einride.tech/aip/resourcename" "ContainsWildcard" }}(request.{{ $resourceGoName }}.Name) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "cannot use wildcard").Err()
            }

            {{- if $rpc.Message.Desc.Fields.ByName "update_time" }}
                // Set the update time.
	              request.{{ $resourceGoName }}.UpdateTime = {{ fqn "google.golang.org/protobuf/types/known/timestamppb" "Now" }}()
            {{- end }}
            {{- if $etag }}
                // Capture the etag.
                etag := request.Get{{ $resourceGoName }}().GetEtag()
            {{- end }}

            // STEP 1: Parse request.
	          parsedRequest, err := update{{ $resourceGoName }}RequestParser.Parse(request)
	          if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "parsing request: %v", err).Err()
	          }

            // STEP 2: retrieve existing resource.
            get{{ $resourceGoName }}Request := &{{ (printf "Get%sRequest" $resourceGoName) | $file.GoImportPath.Ident | qgi }}{ Name: request.{{ $resourceGoName }}.Name }
            existing{{ $resourceGoName }}, err := s.Get{{ $resourceGoName }}(ctx, get{{ $resourceGoName }}Request)
            if err != nil {
            return nil, err
            }
            {{- if $softDeletable }}
            // Verify that the resource is not soft deleted.
            if existing{{ $resourceGoName }}.DeleteTime != nil {
               return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} does not exist").Err()
            }
            {{ end }}

            // STEP 3: Patch the existing resource.
            patched{{ $resourceGoName }} := {{ fqn "google.golang.org/protobuf/proto" "CloneOf" }}(existing{{ $resourceGoName }})
	          parsedRequest.ApplyFieldMask(patched{{ $resourceGoName }}, request.{{ $resourceGoName }})
            if err := {{ fqn "buf.build/go/protovalidate" "Validate" }}(patched{{ $resourceGoName }}); err != nil {
              return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "validating patched resource: %v", err).Err()
            }
            {{ if $etag }}
            { // Compute the new etag.
              var err error
              patched{{ $resourceGoName }}.Etag, err = {{ fqnCore "go/aip" "ComputeETag" }}(patched{{ $resourceGoName }})
              if err != nil {
                 return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "computing new etag: %v", err).Err()
              }
            }
            {{ end }}

            // STEP 4: Insert patched resource.
	          {{ $modelGoName | untitle }}, err := {{ $parseFromPb }}(patched{{ $resourceGoName }})
	          if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from pb to model: %v", err).Err()
	          }
	          db{{ $modelGoName }}, err := s.store.Update{{ $resourceGoName }}(ctx, {{ $modelGoName | untitle }}, parsedRequest.GetSQLUpdateClause(), parsedRequest.GetSQLColumns(){{ if $etag }}, etag{{ end }})
            if err != nil {
            if {{ fqn "errors" "Is" }}(err, {{ $errNotExistFqi }}) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} does not exist").Err()
            }
            {{ if $etag }}
            if {{ fqn "errors" "Is" }}(err, {{ $errEtagChangedFqi }}) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Aborted" }}, "ETag changed").Err()
            }
            {{ end }}
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "inserting {{ $pr.Desc.Singular }}: %v", err).Err()
            }

            {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
		        if err != nil {
			      return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
		        }


            {{ if $natsEventOpts.GetUpdated }}
              // STEP 5: Publish event.
                resourceUpdatedEvent, err := {{ fqnCore "go/aip" "NewResourceUpdatedEvent" }}({{ $resourceGoName | untitle }}, existing{{ $resourceGoName }}, request.GetUpdateMask())
                if err != nil {
                  return nil, status.Errorf(codes.Internal, "constructing resource updated event: %v", err).Err()
                }
                subject := {{ qgi ($file.GoImportPath.Ident (printf "Get%s%s" $service.GoName $natsStreamName)) }}().Subject("updated")
               if err := s.natsClient.Publish(ctx, subject, resourceUpdatedEvent); err != nil {
	                	return nil, status.Errorf(codes.Internal, "publishing resource updated event: %v", err).Err()
	             }
            {{ end }}

            return {{ $resourceGoName | untitle }}, nil
            }

        {{- else if $rpc.Delete }}
            func (s *{{ $serverGoName }}) Delete{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
            // STEP 1: Parse resource name.
            {{ $pr.PatternVariableIDs true }}, err := {{ $parseName }}(request.Name)
            if err != nil {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "parsing name: %v", err).Err()
            }

            {{- if $softDeletable }}
                deleteTime := {{ fqn "time" "Now" }}()
                {{ if $etag }}
                    // Compute new etag.
                get{{ $resourceGoName }}Request := &{{ (printf "Get%sRequest" $resourceGoName) | $file.GoImportPath.Ident | qgi }}{ Name: request.Name }
                {{ $resourceGoName }}, err := s.Get{{ $resourceGoName }}(ctx, get{{ $resourceGoName }}Request)
                if err != nil {
                  return nil, err
                }
                {{ $resourceGoName }}.DeleteTime = {{ fqn "google.golang.org/protobuf/types/known/timestamppb" "New" }}(deleteTime)
                newEtag, err := {{ fqnCore "go/aip" "ComputeETag" }}({{ $resourceGoName }})
                if err != nil {
                    return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "computing etag: %v", err).Err()
                }
                {{ end }}

                // STEP 2: Soft delete the resource.
                db{{ $modelGoName }}, err := s.store.SoftDelete{{ $resourceGoName }}(ctx, {{ $pr.PatternVariableIDs true }}{{ if $etag}}, request.GetEtag(), newEtag{{ end }}, deleteTime)
                if err != nil {
                if {{ fqn "errors" "Is" }}(err, {{ $errNotExistFqi }}) {
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} does not exist").Err()
                }
                {{ if $etag }}
                  if {{ fqn "errors" "Is" }}(err, {{ $errEtagChangedFqi }}) {
                    return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Aborted" }}, "ETag changed").Err()
                  }
                {{ end }}
                if {{ fqn "errors" "Is" }}(err, {{ $errAlreadyDeletedFqi }}) {
                if request.AllowMissing {
                 get{{ $resourceGoName }}Request := &{{ (printf "Get%sRequest" $resourceGoName) | $file.GoImportPath.Ident | qgi }}{ Name: request.Name }
                 return s.Get{{ $resourceGoName }}(ctx, get{{ $resourceGoName }}Request)
                }
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} already deleted").Err()
                }
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "soft deleting {{ $pr.Desc.Singular }}: %v", err).Err()
                }

                // STEP 3: Convert to protobuf and return.
                {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
                if err != nil {
			          return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
                }

            {{ if $natsEventOpts.GetDeleted }}
              // STEP 4: Publish event.
                resourceDeletedEvent, err := {{ fqnCore "go/aip" "NewResourceDeletedEvent" }}({{ $resourceGoName | untitle }})
                if err != nil {
                  return nil, status.Errorf(codes.Internal, "constructing resource deleted event: %v", err).Err()
                }
                subject := {{ qgi ($file.GoImportPath.Ident (printf "Get%s%s" $service.GoName $natsStreamName)) }}().Subject("deleted")
               if err := s.natsClient.Publish(ctx, subject, resourceDeletedEvent); err != nil {
	                	return nil, status.Errorf(codes.Internal, "publishing resource deleted event: %v", err).Err()
	             }
            {{ end }}

                return {{ $resourceGoName | untitle }}, nil

            {{ else }}
                // STEP 2: Hard delete the resource.
                {{ if $natsEventOpts.GetDeleted }}db{{ $modelGoName }}, err :={{ else }}_, err = {{ end }} s.store.Delete{{ $resourceGoName }}(ctx{{ if $pr.PatternVariables }}, {{ $pr.PatternVariableIDs true }}{{ end }}{{ if $etag }}, request.GetEtag(){{ end }})
                if err != nil {
                if {{ fqn "errors" "Is" }}(err, {{ $errNotExistFqi }}) {
                if request.AllowMissing {
                return &{{ fqn "google.golang.org/protobuf/types/known/emptypb" "Empty" }}{}, nil
                }
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} does not exist").Err()
                }
                {{ if $etag }}
                  if {{ fqn "errors" "Is" }}(err, {{ $errEtagChangedFqi }}) {
                    return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Aborted" }}, "ETag changed").Err()
                  }
                {{ end }}
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "deleting {{ $pr.Desc.Singular }}: %v", err).Err()
                }

             {{ if $natsEventOpts.GetDeleted }}
              // STEP 5: Publish event.
                {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
                if err != nil {
			      return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
		        }
                resourceDeletedEvent, err := {{ fqnCore "go/aip" "NewResourceDeletedEvent" }}({{ $resourceGoName | untitle }})
                if err != nil {
                  return nil, status.Errorf(codes.Internal, "constructing resource deleted event: %v", err).Err()
                }
                subject := {{ qgi ($file.GoImportPath.Ident (printf "Get%s%s" $service.GoName $natsStreamName)) }}().Subject("deleted")
               if err := s.natsClient.Publish(ctx, subject, resourceDeletedEvent); err != nil {
	                	return nil, status.Errorf(codes.Internal, "publishing resource deleted event: %v", err).Err()
	             }
            {{ end }}
                return &{{ fqn "google.golang.org/protobuf/types/known/emptypb" "Empty" }}{}, nil
            {{ end }}
            }

        {{- else if $rpc.Get }}
            {{- $resourceDbName := printf "db%s" $resourceGoName }}
            func (s *{{ $serverGoName }}) Get{{ $resourceGoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
            if {{ fqn "go.einride.tech/aip/resourcename" "ContainsWildcard" }}(request.Name) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "cannot use wildcard").Err()
            }

            {{ $pr.PatternVariableIDs true }}, err := {{ $parseName }}(request.Name)
            if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "parsing name: %v", err).Err()
            }

            // Retrieve from the database.
            db{{ $modelGoName }}, err := s.store.{{ $method.GoName }}(ctx, {{ $pr.PatternVariableIDs true }})
	          if err != nil {
            if {{ fqn "errors" "Is" }}(err, {{ $errNotExistFqi }}) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "{{ $pr.Desc.Singular }} does not exist").Err()
            }
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "getting {{ $pr.Desc.Singular }}: %v", err).Err()
	          }

		        {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
		        if err != nil {
			      return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
		        }
            return {{ $resourceGoName | untitle }}, nil
            }

{{- else if $rpc.BatchGet }}

func (s *{{ $serverGoName }}) {{ $method.GoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
    {{ if $pr.Parent }}
        // Validate parent name.
        var {{ $pr.Parent.PatternVariableIDs true }} string
        if request.Parent != "" {
            if err := {{ fqn "go.einride.tech/aip/resourcename" "Sscan" }}(request.Parent, "{{ $pr.Parent.Pattern }}", {{ $pr.Parent.PatternVariableIDPtrs }}); err != nil {
                return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "invalid parent name: %v", err).Err()
            }
        }
    {{ end }}

    // Build WHERE clause for batch retrieval with OR conditions for each full resource identifier
    orClauses := make([]string, len(request.GetNames()))
    whereParams := make([]any, 0, len(request.GetNames()) * {{ len $pr.PatternVariables }})

    for i, name := range request.Names {
        // Validate names.
        if {{ fqn "go.einride.tech/aip/resourcename" "ContainsWildcard" }}(name) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "name cannot contain wildcard").Err()
        }

        {{ if $pr.Parent }}
        // Verify childrren match parent.
        if request.Parent != "" && !{{ fqn "go.einride.tech/aip/resourcename" "HasParent" }}(name, request.Parent) {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "name %q does not have parent %q", name, request.Parent).Err()
        }
        {{ end }}

        // Parse identifiers.
        {{ $pr.PatternVariableIDs true }}, err := {{ $parseName }}(name)
        if err != nil {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "parsing name %s: %v", name, err).Err()
        }

       // Construct where clause.
       conditions := make([]string, {{ len $pr.PatternVariables }})
       {{- range $idx, $id := $pr.PatternVariables }}
         {{- $dbColumn := printf "%s_id" ($id | snakecase) -}}
          {{- if and (eq $idx 0) $modelOpts.IdColumnName }}{{ $dbColumn = $modelOpts.IdColumnName }}{{ end }}
          conditions[{{ $idx }}] = "{{ $dbColumn }} = $" + {{ fqn "strconv" "Itoa" }}(len(whereParams) + {{ add $idx 1 }})
       {{- end }}
       whereParams = append(whereParams, {{ $pr.PatternVariableIDs true }})
       orClauses[i] = "(" + {{ fqn "strings" "Join" }}(conditions, " AND ") + ")"
    }
    whereClause := "WHERE " + {{ fqn "strings" "Join" }}(orClauses, " OR ")

    // Retrieve from the database
    db{{ $pr.PluralGoName }}, err := s.store.List{{ $pr.PluralGoName }}(ctx, {{ if $pr.Parent }}{{ $pr.Parent.PatternVariableIDs true }},{{ end }} {{ if $softDeletable }}true, {{ end }}whereClause, "", "", nil, whereParams...)
    if err != nil {
        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "batch getting {{ $resourceGoName | snakecase }}: %v", err).Err()
    }
    if len(db{{ $pr.PluralGoName }}) != len(request.Names) {
       return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "expected %d {{ $pr.Desc.Plural }}, found %d", len(request.Names), len(db{{ $pr.PluralGoName }})).Err()
    }
    // Convert to proto
    {{ $resourceGoName | untitle }}NameTo{{ $resourceGoName }} := make(map[string]*{{ qgi $rpc.Message.GoIdent }}, len(request.Names))
    for _, db{{ $modelGoName }} := range db{{ $pr.PluralGoName }} {
        {{ $resourceGoName | untitle }}, err := db{{ $modelGoName }}.ToPb()
        if err != nil {
            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting {{ $pr.Desc.Singular }} from model to pb: %v", err).Err()
        }
        {{ $resourceGoName | untitle }}NameTo{{ $resourceGoName }}[{{ $resourceGoName | untitle }}.Name] = {{ $resourceGoName | untitle }}
    }

    {{ $pr.PluralGoName | untitle }} := make([]*{{ qgi $rpc.Message.GoIdent }}, 0, len(db{{ $pr.PluralGoName }}))
    for _, name := range request.Names {
       {{ $resourceGoName | untitle }}, ok := {{ $resourceGoName | untitle }}NameTo{{ $resourceGoName }}[name]
       if !ok {
          return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "NotFound" }}, "could not find %q", name).Err()
       }
       {{ $pr.PluralGoName | untitle }} = append({{ $pr.PluralGoName | untitle }}, {{ $resourceGoName | untitle }})
    }

    return &{{ qgi $method.Output.GoIdent }}{
        {{ $resourceGoName }}s: {{ $pr.PluralGoName | untitle }},
    }, nil
}

        {{- else if $rpc.List }}
            var {{ $method.Input.GoIdent.GoName | untitle }}Parser = {{ fqnCore "go/aip" "MustNewListRequestParser" }}[*{{ qgi $method.Input.GoIdent }}, *{{ qgi $rpc.Message.GoIdent }}]()

            func (s *{{ $serverGoName }}) {{ $method.GoName }}(ctx {{ fqn "context" "Context" }}, request *{{ qgi $method.Input.GoIdent }}) (*{{ qgi $method.Output.GoIdent }}, error) {
            {{- $resourceDbName := printf "db%s" $resourceGoName }}
            {{ if $pr.Parent }}
                // Parse parent names
                var {{ $pr.Parent.PatternVariableIDs true }} string
                if err := {{ fqn "go.einride.tech/aip/resourcename" "Sscan" }}(request.Parent, "{{ $pr.Parent.Pattern }}", {{ $pr.Parent.PatternVariableIDPtrs }}); err != nil {
		            return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, "invalid parent name: %v", err).Err()
                }
            {{ end }}

            // Parse request
            parsedRequest, err := {{ $method.Input.GoIdent.GoName | untitle }}Parser.Parse(request)
	          if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "InvalidArgument" }}, err.Error()).Err()
	          }
	          whereClause, whereParams := parsedRequest.GetSQLWhereClause()
            var dbColumns []string

            // Retrieve from the database.
            {{ $resourceDbName }}s, err := s.store.{{ $method.GoName }}(ctx, {{ if $pr.Parent }}{{ $pr.Parent.PatternVariableIDs true }},{{ end }} {{ if $softDeletable }}request.ShowDeleted, {{ end }}whereClause, parsedRequest.GetSQLOrderByClause(), parsedRequest.GetSQLPaginationClause(), dbColumns, whereParams...)
	          if err != nil {
		        return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "listing {{ $resourceGoName | snakecase }}: %v", err).Err()
	          }
	          nextPageToken := parsedRequest.GetNextPageToken(len({{ $resourceDbName }}s))
	          if nextPageToken != "" {
		        {{ $resourceDbName }}s = {{ $resourceDbName }}s[:len({{ $resourceDbName }}s)-1]
	          }

            // Convert back to proto.
            {{ $pr.PluralGoName | untitle }} := make([]*{{ qgi $rpc.Message.GoIdent }}, 0, len({{ $resourceDbName }}s))
	          for _, {{ $resourceDbName }} := range {{ $resourceDbName }}s {
		        {{ $resourceGoName | untitle }}, err := {{ $resourceDbName }}.ToPb()
		        if err != nil {
			      return nil, {{ fqnCore "go/grpc/status" "Errorf" }}({{ fqn "google.golang.org/grpc/codes" "Internal" }}, "converting model.{{ $resourceGoName }} to {{ $resourceGoName }}: %v", err).Err()
		        }
		        {{ $pr.PluralGoName | untitle }} = append({{ $pr.PluralGoName | untitle }}, {{ $resourceGoName | untitle }})
	          }

            // Create and return response.
            return &{{ qgi $method.Output.GoIdent }}{
            {{ $pr.PluralGoName }}: {{ $pr.PluralGoName | untitle }},
            NextPageToken: nextPageToken,
            }, nil
            }
        {{ end -}}
    {{ end }}
{{- end }}
