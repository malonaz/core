local google = import 'google.json';
{
  provider_id: 'google-vertex',
  models: [
    model {
      name: std.strReplace(model.name, 'providers/google/', 'providers/google-vertex/'),
    }
    for model in google.models
  ],
}
