name: scheduler-service
target: //go/scheduler/scheduler_service
codegens:
  - rpc:
      name: scheduler-service
      store: scheduler
      target: //gengo/scheduler/scheduler_service/rpc
dependencies:
  - postgres_db_client:
      name: scheduler
      target: //gengo/scheduler/store
